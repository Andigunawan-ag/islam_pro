// lib/services/api_service.dart

//import 'dart:convert';
//import 'package:http/http.dart' as http;
//import '../models/comic.dart';

//class ApiService {
  // Ubah URL ini sesuai dengan URL API lokal Anda
 // final String apiUrl = 'http://localhost:3000/api/';  // Contoh API lokal, ganti sesuai API Anda

  //Future<List<Comic>> fetchComics() async {
    //try {
      //final response = await http.get(Uri.parse(apiUrl));

      //if (response.statusCode == 200) {
       // final List<dynamic> data = json.decode(response.body);
        //return data.map((comicData) => Comic.fromJson(comicData)).toList();
      //} else {
       // throw Exception('Failed to load comics');
      //}
    //} catch (e) {
     // throw Exception('Failed to load comics: $e');
    //}
  //}
//}
