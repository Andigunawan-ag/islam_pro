name: Islam_Pro
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  cached_network_image: ^3.2.0
  flutter_staggered_grid_view: ^0.6.1
  intl: ^0.17.0 # Menambahkan dependensi intl untuk format tanggal
  flutter_launcher_icons: ^0.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  flutter_icons:
    android: true
    ios: true
    image_path: "assets/icon/islampro.png"
    min_sdk_android: 21


flutter:
  uses-material-design: true
